# Data Repository Path
# Path to the data repository containing definitions/ and where generated/ will be written
DATA_REPO_PATH=../buildright-data

# Note: All project-specific configuration (project name, store codes, attribute prefixes, etc.)
# is defined in the data repository's definitions/project.json file.

